# Engine environment defaults for external market feed
# Set EXTERNAL_FEED=binance to enable Binance ingestor; otherwise DEMO_MODE can drive synthetic publisher
EXTERNAL_FEED=binance
EXTERNAL_MARKET=BTCUSDT
MARKET_ALIAS=BTC_USDC
DEMO_MODE=false

# Redis defaults inherited by RedisManager (if any are used elsewhere, add here)
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
